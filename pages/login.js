import Router from "next/router";

export default function Login() {
    const router = Router();
    
    return (
        <div>
            No autenticado
            <Button label="Iniciar session"/>        
        </div>
    )
}